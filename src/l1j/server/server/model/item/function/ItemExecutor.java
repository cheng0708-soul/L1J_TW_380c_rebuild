package l1j.server.server.model.item.function;

/**
 * Bridge interface to retain old imports.
 * One-way extends the canonical executor interface to avoid cycles.
 */
public interface ItemExecutor extends l1j.server.server.model.item.executor.ItemExecutor {
    // no extra methods
}
